# Internationalisation (i18n) – Application bilingue

**Dernière mise à jour :** 12 mai 2026 (**`coachMsaOffers`** — modale choix d’offre plateforme + **`byPriceId`** + référence **`futurePlatformPaymentsTier`** pour offre à venir ; précédent : **`coachMsaSubscription`** seul…)
**Langues supportées :** Français (fr) – défaut, Anglais (en)  
**Bibliothèque :** next-intl 3.x  
**Statut :** ✅ Mise en place bilingue complétée

---

## 🎯 Règle d'or : Toujours penser bilingue

**Toute nouvelle feature ou modification de texte visible par l'utilisateur doit être traduite dès le départ.**

- Ne pas introduire de chaînes en dur (français ou anglais) dans les composants ou les server actions.
- Ajouter les clés dans `messages/fr.json` et `messages/en.json` en même temps que le code.
- Utiliser les namespaces existants ou en créer un si le domaine le justifie.

Consulter la **Checklist nouvelles features** plus bas pour ne rien oublier.

---

## 📋 Résumé de l'implémentation

- **Infrastructure :** next-intl, middleware (intégré dans `proxy.ts`), structure `i18n/` et `messages/`
- **Routing :** Routes sous `app/[locale]/` ; API et auth restent à la racine
- **Fichiers de traduction :** `messages/fr.json`, `messages/en.json` (namespaces : common, auth, navigation (dont **contactUs**, **`coachPlatformSubscription`** libellé menu Mon Abonnement MySportAlly), **contact** (formulaire public + erreurs serveur), **landing.footer.contactLink**, metadata **contactTitle** / **contactDescription**, sports (dont libellés distincts ski de fond / ski de randonnée / patinage sur glace), workouts, **athleteStats** (volume réalisé, filtres, légende axe Y `chart.yAxisLegend*`, tooltip tranche, volume annuel / encart années, unités, erreurs), **goals** (dont **calendarTabObjectives** — onglet Objectifs sous calendrier coach), profile, coach, devices, chat, errors, metadata, calendar, **availability** (disponibilités athlète : type, start, end, note, fullDay, modales), **facilities** (installations : profil + **edit** / **delete**, **calendarTabFacilities**, types, adresse, horaires, modal, validation), **coachAthleteNotes** (notes privées : **calendarTabNotes**, liste, modale, **validation**), **coachMsaSubscription** (page abonnement plateforme coach : statut, offres, historiques facturation, textes portail absent v1), **`coachMsaOffers`** (modale choix d’offre + **`byPriceId`** titres/descriptions par prix Stripe), etc. — ex. `calendar.today` pour le badge « Aujourd'hui »)
- **Composant :** `LanguageSwitcher` (basé sur **Dropdown**) sur la page profil et dans **`PublicHeader`** (visiteur : accueil, contact, confidentialité, CGU, reset-password, etc.) pour basculer FR/EN ; **utilisateur connecté** sur ces routes voit **`DashboardTopBar`** (pas le switch dans le même bandeau que le visiteur — cohérence avec le dashboard). aria-label via `common.changeLanguage` ; enregistrement de `preferred_locale` en BD
- **Emails d’authentification (hors app) :** les templates Supabase (ex. confirmation d’inscription) sont bilingues via la metadata ` .Data.locale` passée au signup ; template et variables décrits dans `docs/AUTH_EMAIL_TEMPLATES.md`

---

## 🗂️ Structure des fichiers

```
/i18n
  ├── request.ts      # Configuration i18n
  └── routing.ts      # Locales (fr, en), defaultLocale, localePrefix
/messages
  ├── fr.json         # Traductions françaises
  └── en.json         # Traductions anglaises
/app
  ├── [locale]/       # Routes localisées (page, dashboard, login, etc.)
  ├── api/            # API routes (non localisées)
  └── auth/           # Auth callbacks (non localisées)
proxy.ts              # Middleware : next-intl + Supabase auth
```

---

## 🎯 Utilisation dans le code

### Server Component

```typescript
import { useTranslations } from 'next-intl';

export default function MyPage() {
  const t = useTranslations('navigation');
  return <h1>{t('dashboard')}</h1>;
}
```

### Client Component

```typescript
'use client';
import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations('common');
  return <button>{t('save')}</button>;
}
```

### Server Actions (messages d'erreur / succès)

```typescript
'use server';
import { getTranslations } from 'next-intl/server';

export async function myAction(formData: FormData) {
  const locale = (formData.get('_locale') as string) || 'fr';
  const t = await getTranslations({ locale, namespace: 'auth.errors' });
  if (!user) return createError(t('notAuthenticated'), 'AUTH_REQUIRED');
  return createSuccess(data);
}
```

Transmettre la locale depuis le formulaire : `formData.append('_locale', locale)` (avec `useLocale()` côté client).

### Labels de sports dynamiques

```typescript
import { SPORT_TRANSLATION_KEYS } from '@/lib/sportStyles';
import { useTranslations } from 'next-intl';

const t = useTranslations();
const sportLabel = t(SPORT_TRANSLATION_KEYS[sportType]);
```

---

## 🌐 URLs et routing

- **Locale par défaut :** Français (fr) – pas de préfixe : `/dashboard`
- **Anglais :** Préfixe `/en` : `/en/dashboard`
- **Middleware :** Détection de la langue du navigateur, redirections avec conservation de la locale (proxy combine next-intl + Supabase auth).

---

## 📝 Namespaces de traduction

| Namespace   | Usage principal                          |
|------------|-------------------------------------------|
| `common`   | save, cancel, delete, edit, close, retry  |
| `auth`     | login, signup, **showPassword**, **hidePassword** (bouton afficher/masquer mot de passe), **emailValidatedTitle**, **emailValidatedMessage** ; **auth.errors** : userExists, accountCreatedSuccess, confirmationEmailResent, accountExistsLoginMessage, confirmationLinkFailed (login après erreur callback) |
| `navigation` | Menu : dashboard, calendar, goals, **myInformation**, **accountMenu**, **coachPlatformSubscription** (entrée **Mon Abonnement MySportAlly** côté coach), **contactUs**, **publicHome** / **publicPrivacy** / **publicTerms** / **resetPasswordPage** (titre mobile top bar sur pages marketing lorsque l’utilisateur est connecté), etc. |
| `sports`   | Labels des sports                         |
| `workouts` | Titre, description ; **validation** (dont **allFieldsRequired**, **invalidSport**, **onlyCoachCanCreate** / **onlyCoachCanEdit** / **onlyCoachCanDelete**, **notAuthenticated**, **unauthorized**, **commentSaveFailed**, **notFound**, **deleteSuccess**, **targetRequired**, **durationRequiredForSport** — sports temps seul ex. musculation, **paceRequired**, **primaryMetricDistanceRequired** / **primaryMetricTimeRequired** — métrique obligatoire coach selon profil, **workoutUnitsNotConfigured**) : utilisées côté serveur pour les erreurs de **`validateWorkoutFormData`** après mapping par **`translateWorkoutFormValidationError`** (`lib/workoutValidation.ts`, actions `createWorkout` / `updateWorkout`) ; **facilityHoursStripAriaLabel** ; **form.timeOfDay**… ; **calendar.morning** / **noon** / **evening** ; **feedback** (retour athlète) |
| `goals`    | Objectifs ; **result** (time, hours/minutes/seconds, placeOptional, noteOptional, addResult, editResult, resultSection, noResult) ; **validation** (goalNotFound, missingGoalId, resultOnlyForPastGoal, invalidTimeRange, targetTimeAllRequired, noteMaxLength, resultSaved) — *`timeRequired` retiré : la saisie du temps résultat n’impose plus les trois champs ; champs vides = 0 si au moins un temps saisi* |
| `profile`  | Profil utilisateur ; **weeklyTargetSectionTitle**, **weeklyTargetLabel**, **noPracticedSportsMessage**, **suffixHoursPerWeek** / **suffixKmPerWeek** / **suffixMPerWeek** (objectifs et volume par sport, athlète) ; **validation.weeklyTargetInvalid**, **validation.weeklyVolumeInvalid** |
| `coach` / `myCoach` | Mon coach, notation              |
| `devices`  | Appareils connectés (Strava)               |
| `chat`     | Chat ; **validation** : **coachPlatformRequired** (coach sans abonnement plateforme) |
| `errors`   | Erreurs globales                           |
| `metadata` | Titres de pages (SEO)                     |
| `calendar` | Calendrier : today, days, **weekly** (dont **planned**, libellés **sportLabels.run**, totaux détaillés `runningDistanceCompletedPlanned`…, **`sportVolumeHint`** `{sport}` pour tooltips sports hors libellés longs), modals, tile.athleteCommentLabel (tooltip/aria-label icône commentaire athlète sur tuile), **weekRangeSeparator** (séparateur de plage de dates WeekSelector), **prevMonth** / **nextMonth** (aria-labels MonthSelector, desktop `md`+) |
| `findCoach` | Page Trouver mon coach : titre, filtres, résultats, tuiles ; **reviewsModal** : modale liste des avis (titre, chargement, erreur, vide, pas de commentaire, aria) ; **requestGoals** : section objectifs de course / résultats passés dans la demande (titres, Ajouter, Voir plus, Modifier le résultat, modales) |
| `requestCoachButton` | Tuile coach : Demande envoyée, Annuler la demande, formulaire demande |
| `athleteSentRequest` | Modale détail demande envoyée (titre, envoyée à, offre, sports, message, Fermer) |
| `athletes` | Mes athlètes (coach) : completeProfilePrompt, **publishOfferPrompt** (bandeau si aucune offre publiée), pendingRequests (title, chat, **athleteMessageLabel**, **objectivesAndVolumeLabel**, **notSpecified**), filtre, sortBy*, athleteCard, noAthletes, **listLoadError** (échec chargement liste profils) |
| `coachRequests` | accept, decline, confirmDeclineTitle/Body, confirmAcceptTitle/Body ; **validation** : requireWeeklyTargetAndVolume, weeklyTargetInvalid, weeklyVolumeInvalid, **platformSubscriptionRequired** (acceptation sans abonnement plateforme) |
| `coachPlatform` | Abonnement Stripe coach → plateforme : bandeaux retour checkout (`stripeReturn*`, `stripeVerifying`, `stripeReturnPendingWebhook`, `stripeRefreshCta`), blocage liste/calendrier, modale avant paiement, CTA ; **validation** (checkout, config Stripe, **`invalidPriceId`**) ; **verify** (messages serveur `verifyCoachPlatformCheckoutSession`) |
| `coachMsaSubscription` | Page **`/dashboard/coach-platform-subscription`** : titres sections (offres, factures, paiements échoués, remboursements), statuts d’abonnement, bandeau grâce, libellés intervalle de prix, absence de portail client v1 (`portalHint`), erreurs chargement historique |
| `coachMsaOffers` | Modale **choix d’offre** avant Checkout : `modalTitle`, `modalIntroDefault`, `loading`, `close`, `noOffers` ; **`byPriceId`** : objet dont les clés sont les `price_id` Stripe (`title`, `description`) — repli sur nom / description produit Stripe si clé absente. **`futurePlatformPaymentsTier`** : textes de référence (mensuel / annuel) pour une future offre « plateforme + encaissements athlètes » — **non utilisés par l’UI** tant qu’il n’existe pas de `price_id` ; au lancement, recopier les libellés sous `byPriceId.<nouveau_price_id>` (FR + EN). |
| `offers` | title, pricing, priceTypes, status (draft/published/archived), priceLockedHint, **pricingLockedBadge** (« Non modifiable »), **publishModal** (title, message, confirm — message : prix non modifiable après publication, corrections typos titre/description uniquement), archiveModal, validation |
| `facilities` | Installations : section Mon profil, **edit** / **delete**, **calendarTabFacilities** (onglet sous calendrier coach), types (`piscine/salle/stade/autre`), champs adresse, jours, horaires (open/closed), labels modal add/edit, **deleteConfirmation**, validation server-side (`facilities.validation.*`) |
| `contact` | Page publique **Contact** : titres, labels, `reasons.*`, `reasonPlaceholder`, `submit` / `submitting`, **`privacyConsentNotice`** (texte riche avec balise `<privacy>` → lien `/privacy` via `t.rich`), `success.*` ; **`contact.errors`** (validation formulaire + serveur) : `firstNameInvalid`, **`firstNameTooLong`**, `lastNameInvalid`, **`lastNameTooLong`**, `emailInvalid`, `reasonInvalid`, `messageInvalid`, **`messageTooLong`**, `submitFailed`, **`emailNotifyUnavailable`** (clé Resend absente côté serveur), `emailSendFailed` — paramètre `{max}` pour les clés *TooLong |

Ajouter un namespace dans les deux fichiers `fr.json` et `en.json` si un nouveau domaine le justifie.

---

## ✅ Checklist : Nouvelles features (toujours penser bilingue)

À chaque nouvelle feature ou modification de texte :

1. **Composants (Server ou Client)**
   - [ ] Aucun texte en dur : utiliser `useTranslations('namespace')` et `t('key')`.
   - [ ] Clés ajoutées dans `messages/fr.json` et `messages/en.json` (même structure).

2. **Formulaires et Server Actions**
   - [ ] Passer la locale aux actions : `formData.append('_locale', locale)` (récupérer `locale` avec `useLocale()`).
   - [ ] Messages d'erreur / succès traduits via `getTranslations({ locale, namespace })` dans l’action.
   - [ ] **Workouts coach :** si la validation est factorisée avec des codes (`errorCode`), ne pas renvoyer la chaîne technique brute au client : utiliser le même namespace **`workouts.validation`** (ex. `translateWorkoutFormValidationError`) et **`getLocale()`** si le formulaire n’envoie pas `_locale`.

3. **Pages**
   - [ ] Titres et meta : utiliser les clés du namespace `metadata` (ou équivalent) avec la locale.

4. **Nouveau domaine (optionnel)**
   - [ ] Si besoin, créer un namespace dédié dans `fr.json` et `en.json` (ex. `myFeature`) et l’utiliser de façon cohérente.

5. **Vérifications rapides**
   - [ ] Les deux fichiers JSON ont les mêmes clés pour le namespace utilisé.
   - [ ] Tester le flux en FR et en EN (changement de langue via `LanguageSwitcher`).

---

## ➕ Ajouter une nouvelle langue

1. Créer `messages/xx.json` (copier depuis `en.json` et traduire).
2. Mettre à jour `i18n/routing.ts` : ajouter la locale dans `locales`.
3. Mettre à jour le matcher dans `proxy.ts` si nécessaire.
4. Ajouter l’option dans le composant `LanguageSwitcher`.

---

## 🐛 Dépannage

- **Traductions manquantes :** Vérifier que la clé existe dans les deux fichiers et que le namespace est correct (`t('namespace.key')` ou `useTranslations('namespace')` puis `t('key')`).
- **Routing :** Les routes utilisateur sont sous `app/[locale]/` ; ne pas mettre les routes API/auth dans `[locale]`.
- **Params Next.js 16 :** Dans les layouts/pages, `params` est une Promise : `const { locale } = await params;`.

---

## 📚 Références

- [Documentation next-intl](https://next-intl.dev/)
- [App Router setup](https://next-intl.dev/docs/getting-started/app-router)

---

**Document de référence pour toute évolution de l’application. Penser bilingue dès la conception des nouvelles features.**
